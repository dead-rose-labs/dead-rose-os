# The installed-image smoke test captures ttyS0 while running without a display.
# Keep this test-only override after the product defaults in lexical order.
GRUB_CMDLINE_LINUX_DEFAULT="${GRUB_CMDLINE_LINUX_DEFAULT} console=tty0 console=ttyS0,115200n8"
