[terminal]
vt = 1
switch = true

[initial_session]
command = "/usr/bin/cage -- /opt/dead-rose/bin/dead-rose-shell"
user = "deadrose"

[default_session]
command = "/usr/sbin/agreety --cmd /bin/sh"
user = "_greetd"
