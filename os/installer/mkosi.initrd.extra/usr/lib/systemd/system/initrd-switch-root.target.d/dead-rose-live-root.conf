[Unit]
# Keep the native initrd mount units in the isolated switch-root transaction.
# Explicit unit dependencies survive generator reloads after /sysroot appears.
Requires=run-dead\x2drose\x2diso.mount sysroot.mount
After=run-dead\x2drose\x2diso.mount sysroot.mount
