[Unit]
# Keep the read-only live root and its loop backing file in the isolated
# switch-root transaction. Depending only on initrd filesystem targets is not
# sufficient on all systemd builds: mount units may otherwise be stopped after
# initrd cleanup and /sysroot disappears before initrd-switch-root.service.
RequiresMountsFor=/run/dead-rose-iso /sysroot
